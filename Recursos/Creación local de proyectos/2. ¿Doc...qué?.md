sí así es usaremos docker para levantar una instancia de PostgreSQL ya que esto hará que nuestros equipos estén en optima seguridad y la ventaja de esto es que no necesitas instalar el motor de la base de datos directamente en tu computadora, manteniendo tu entorno limpio.

Para hacer esto, crea un archivo llamado `docker-compose.yml` en la raíz de tu proyecto (exactamente al mismo nivel donde está tu archivo `package.json`) y pega el siguiente código: